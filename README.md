# Startup_Git
Reminder on what to do with a new linux sys

Set Username: 
git config --global user.name "Your Name".
Set Email: 
git config --global user.email "your.email@example.com".
Verify: Run git config --list to confirm the settings are saved
